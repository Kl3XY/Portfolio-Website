on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    environment: build
    steps:
      - name: display env vars
        run: |
          echo $TEST_VARIABLE
        env:
          ENVIRONMENT_STAGE: ${{ vars.TEST_VARIABLE }}


